services:
  permission_fixer:
    image: alpine
    volumes:
      - ../iotGreenhouse:/iotGreenhouse
    command: >
      sh -c "chmod +x /iotGreenhouse/database/schema.sh"
  shared_db_container:
    image: mysql:8.0
    environment:
      - MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD}
      - MYSQL_DATABASE=${DB_NAME}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
    depends_on:
      permission_fixer:
        condition: service_completed_successfully
    volumes:
      - db_data:/var/lib/mysql
      - ./database/schema.sh:/docker-entrypoint-initdb.d/01-init-db.sh
    networks:
      - iot-network
  greenhouse-service:
    build: 
      context: .
      args:
      - REACT_APP_GREEN_API_URL=${REACT_APP_GREEN_API_URL}
    environment:
      - TZ=America/Toronto
      - API_PORT=${API_PORT}
      - BIND_HOST=${BIND_HOST}
      - DB_HOST=${DB_HOST}
      - DB_NAME=${DB_NAME}
      - DB_USER=${DB_USER}
      - DB_PASS=${DB_PASS}
      - STATIC_FOLDER=${STATIC_FOLDER}
    restart: always
    ports:
      - "${GREEN_API_PORT}:${API_PORT}"   
      - "${COLLECTOR_HOST_PORT}:${COLLECTOR_PORT}"
    env_file: .env
    depends_on:
      - shared_db_container
    networks:
      - iot-network

networks:
  iot-network:
    driver: bridge
    name: iot-network

volumes:
  greenhouse_db_data: